import React, { Component } from 'react'
import {Button } from 'react-bootstrap';

export default class MyButton extends Component {
    render() {
        return (
           <Button variant="secondary" > Heyo </Button>
        )
    }
}
